on: 
  push:
    branches:
      - juanpc10-patch-1
name: deploy-dev
jobs:
  deploy:
    name: build-deploy-dev
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
      with:
        ref: juanpc10-patch-1
    - name: build-deploy
      run: |
        ~/build-deploy-2
